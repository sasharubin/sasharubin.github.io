ls -lh filename 
will show size in readable form

pdftk inputfiles cat output targetfile.pdf
will concatenate all files and put them in targetfile.pdf

for f in *; do mv "$f" "$f.jpg"; done
for adding extensions


